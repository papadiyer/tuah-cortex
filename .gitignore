.m5/
__pycache__/
*.pyc
*.db
# SQLite WAL sidecars (the durable queue runs in WAL mode)
*.db-wal
*.db-shm
.DS_Store

# Service runtime logs
logs/*.log

# Generated from the real Hermes corpus - contains private conversation text.
data/hermes_export.jsonl

# Local Graphify knowledge-graph output (generated, not source)
graphify-out/

# Python virtualenv for the semantic embedder (sentence-transformers, 3.11).
# Built per-host via the Proxmox runbook; never commit (env-specific, 470MB+ model).
.venv-cortex-st/

# Lexical->semantic migration backup of the old vector store (local artifact).
*.bak
