# Jebat-Cortex — Task Log 06082026_195x (Graphify repo-brain layer)

## Goal (Faisal)
Dapatkan Graphify working supaya Lekiu/Codex/Claude Code dapat structured
repo context masa delegate work. Procedure: Search & Verify dulu, jumpa source
tepat yg match `query --repo --topic`, install dalam venv sahaja, verify jalan
dgn graphify_context.sh, then re-run Jebat-Cortex ingest. No blind install.

## Verify findings (root-cause, not assumed)
- `graphify` TIADA on PATH / pip / module sebelum ni. Cuma wrapper
  `graphify_context.sh` (skill delegate_coding_to_lekiu).
- Official repo: Graphify-Labs/graphify (Apache 2.0). Faisal sahkan.
- REAL CLI (from source cli.py): `graphify query "<question>" [--dfs]
  [--context C] [--budget N] [--graph path]`. Queries existing graph.json.
  NO `--repo` / `--topic` flags. Wrapper kita punya invocation SALAH.
- PyPI package = `graphifyy` (double-y), NOT `graphify`. System Python 3.9.6
  tapi pkg needs >=3.10 -> guna python3.11 venv.
- Extract needs LLM key for non-code files; `--code-only` works FULLY LOCAL
  (tree-sitter AST, no key). Perfect for repo-brain layer.

## Actions
1. Clone Graphify-Labs/graphify (verify source). Confirmed interface mismatch.
2. python3.11 -m venv .venv-graphify; pip install graphifyy (0.9.34). OK.
3. Smoke test: `graphify extract --code-only . --no-viz` -> 398 nodes, 859
   edges, 22 communities (local, no key). `graphify query "..."` -> structured
   context. OK.
4. Patch graphify_context.sh run_graphify(): build graph (extract --code-only)
   if graph.json missing, then `graphify query "$TOPIC"`. Removed wrong
   `--repo/--topic` + python module fallback. Fixed echo label.
5. Symlink .venv-graphify/bin/graphify -> ~/.hermes/bin/graphify (on PATH for
   delegation).
6. .gitignore: add graphify-out/ + .venv-graphify/.
7. Test wrapper E2E: `graphify_context.sh <repo> <topic>` -> real findings. OK.
8. Re-run run_cortex.sh --from-hermes: read-only proven (state.db unchanged),
   digest OK. 112 tests green.

## Cognitive stack (Faisal)
Jebat/Hermes -> Graphify (repo knowledge graph) -> Lekiu/Codex/Claude Code.
Graphify = repo brain layer, not full conversational memory. Complements
Jebat-Cortex (experience/knowledge memory).

## Caveats
- graphify-out/ + .venv-graphify/ gitignored, tak leak.
- Symlink graphify ke ~/.hermes/bin: reversible (rm symlink). venv isolated.
- Wrapper edit kat skill dir (delegate_coding_to_lekiu) - ikut procedure Bos.
- Belum commit (Bos tak suruh commit); changes local.
