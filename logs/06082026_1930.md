# Jebat-Cortex — Task Log 06082026_19xx (v0.3 P1 final gap: retrieval fail-loud)

## Third-round review (Faisal) — final gap
- Data mixing dicegah (add refuses incompatible; query skips). 110/110 green.
- BUT: new tests EXPECT query() return kosong -> tiada test yang paksa
  PRODUCTION ContextBuilder fail loud. Retrieval masih gagal secara senyap
  bila identity berubah: row lama hilang dari result, tiada error.
- Ask: enforce compatibility SEBELUM retrieval (fail fast), then green light.

## Root-cause (Jebat, confirmed)
ContextBuilder.retrieve() called self.vector.query() direct tanpa compatibility
gate. query() defensively skip incompatible rows -> knowledge=[] -> build()
emit digest kosong, no error. Silent memory-loss in production.

## Fix (final)
- core/context_builder.py: retrieve() now calls
  self.vector.check_compatibility(raise_on_mismatch=True) BEFORE query().
  Production retrieval fails loud on any backend/model/dim mismatch. Covers
  ST-fallback path too (same VectorStore).
- Tests (test_context_builder.py): TestRetrievalFailsLoudOnIdentityMismatch
  - build() raises ValueError on mismatch
  - retrieve() raises before query
  (112 tests now; was 110 + 2).

## Validation (this turn, ad-hoc hermes-verify-*.py)
- PRODUCTION ContextBuilder.build() on model-a->model-b (same dim):
  FAILED LOUD (ValueError), NOT silent-empty. GOOD.
- same-identity retrieval still returns 1 knowledge row. GOOD.
- Full suite: 112 tests OK.

## Result
- Both P1 FULLY resolved (mixing prevented + retrieval fails loud). v0.3 ready.
- Committed locally. Still NO push (Faisal not green-lighted push).

## Caveats
- No push pending Faisal approval.
