# Jebat-Cortex — Task Log 06082026_18xx (v0.3 P1 fixes)

## Directive (Faisal)
Tahan v0.3. Selesaikan 2 P1 dari review diff 23e5a0c..a55eaf5 sebelum terus:
- P1 #1 (vector_store.py:50-59): backend change silently mixes incompatible
  vectors (campuran dim [4,3]; old memory senyap dari hasil).
- P1 #2 (ingest_hermes.py:263-269): tool-only structured payload retained as
  memory (reproducer: raw JSON incl. 'thinking' content).
Plus: add regression tests for both. No push.

## Root-cause verification (Jebat, not assumed)
- P1 #1: vector_store embeds via active embedder but stores NO backend tag;
  cosine() returns 0 on dim mismatch, so old rows silently unrank. Docstring
  admitted it. No enforcement. Confirmed.
- P1 #2: _clean_content fell through to raw text when _text_from_structured
  returned "" for unrecognised JSON; tool envelopes (type=tool_use/result/
  thinking) had no human text -> returned "" -> BUT caller kept raw because
  `if extracted:` was falsy. Reproduced: tool_result+thinking -> raw JSON kept.

## Fixes applied
- core/vector_store.py: add embed_meta column (backend+dim) per row; migrate
  old dbs via _ensure_embed_meta(); query() skips dim-mismatched rows;
  add check_compatibility(raise_on_mismatch=) reporting + loud ValueError;
  curator.ingest() calls it (raise_on_mismatch=True) -> backend flip fails
  loud, not silent.
- core/ingest_hermes.py: _text_from_structured uses 3-state walk (1=found
  text, 0=tool/thinking envelope -> drop, -1=unrecognised -> keep raw). Only
  genuine tool envelopes are dropped; unrecognised JSON kept (old behaviour).

## Regression tests added (Faisal requested)
- tests/test_vector_store.py: TestBackendMismatchIsNotSilent (query skips
  mismatch + keeps new; check_compatibility reports + raises; rows tagged).
- tests/test_ingest_hermes.py: TestToolOnlyPayloadDropped (tool_result+
  thinking, tool_use, list-of-tools, thinking-alone -> dropped; human text
  kept; non-JSON bracket string kept verbatim).

## Validation (this turn, ad-hoc hermes-verify-*.py)
- P1#2 tool-only payload -> "" (dropped)
- P1#2 unrecognised JSON kept verbatim (regression avoided)
- P1#1 old 512-d row NOT in results; check_compatibility mismatched=1, raises
- Full suite: 107 tests OK (was 98 + 9 new)
- Real --from-hermes run: read-only proven, digest under budget, 0 degenerate

## Result
- Both P1 fixed + regression-locked. v0.3 ready to hold. Committed locally.

## Caveats
- No push (Faisal directive).
- sentence-transformers still opt-in (not installed); dim-mix guard now active
  if a backend flip is attempted.
