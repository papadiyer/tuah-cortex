# Jebat-Cortex — Task Log 06082026_190x (v0.3 P1 round-2)

## Second-round review (Faisal) — 1-of-2 P1 still open
Faisal re-verified: P1#2 (tool payload) resolved. P1#1 (vector) INCOMPLETE:
- query() only rejected on dim mismatch; same-dim different backend/model
  still ranked (row A returned score 1.0).
- add() never checked compatibility -> could inject row backend B into a DB
  with row backend A (dim sama).
- SentenceTransformerEmbedder.name sama untuk semua model; embed_meta tk
  tiada model_name -> same-dim model swap undetectable.

## Root-cause (Jebat, confirmed)
Embedding identity only carried {backend, dim}. No model. query() skipped on
dim only. add() no gate. So same-dim different-space rows silently mixed.

## Fix (round-2, root level)
- core/rules.py: Embedder.model property (deterministic->"deterministic";
  SentenceTransformerEmbedder.model -> self.model_name). Full identity =
  backend + model + dim.
- core/vector_store.py:
  * embed_meta now {backend, model, dim}.
  * _scan_identity() at open: captures store identity; raises on mixed store.
  * add(): refuses (ValueError) if row identity != store identity -> enforced
    at API boundary, not just via curator.
  * query(): skips rows whose full identity != active (backend/model/dim).
  * check_compatibility(): compares full identity; raises on mismatch.
- Tests: _FakeEmbedder gains model arg; added same-dim different-model cases:
  query skip + add refusal + check_compatibility model mismatch; tag carries
  model. (110 tests now; was 107 + 3 new.)

## Validation (this turn, ad-hoc hermes-verify-*.py)
- same-dim diff-model query returns row A? False (no score 1.0)
- add() refused incompatible identity: GOOD
- check_compatibility mismatched=1, active_model=model-b
- dim-only mismatch still skipped (regression kept)
- P1#2 still holds (tool dropped / human kept / unknown JSON kept)
- Full suite: 110 tests OK

## Result
- Both P1 fully resolved. v0.3 ready. Committed locally. Still NO push
  (Faisal not green-lighted push).

## Caveats
- No push pending Faisal approval.
- sentence-transformers opt-in (not installed); model-name gate now active
  for same-dim model swaps.
