# Jebat-Cortex — Task Log 06082026_1749 (v0.2)

## Objective
v0.2: ingest REAL Hermes conversation corpus (read-only ~/.hermes/state.db)
into the v0.1 pipeline + make the embedder pluggable (deterministic default,
optional sentence-transformers, no forced dep).

## Prep (Jebat)
- Read actual v0.1 code: rules.py, memory_curator.py, cortex_rules.json,
  run_cortex.sh — confirmed architecture + async ingest(log_path).
- Inspected ~/.hermes/state.db schema (read-only):
  - messages(id, session_id, role, content, tool_call_id, tool_calls,
    tool_name, timestamp REAL, token_count, ...)
  - sessions(id, source, user_id, model, started_at, ended_at, ...)
- Confirmed (i) ingest real sessions = approved by Faisal. (ii) embedding =
  pluggable default deterministic (Bos did not answer; Jebat chose safe default).
- Trust flag + allowlist already set from v0.1 run 2.

## Actions
- Wrote .m5/tasks/current-lekiu-task-v02.md (scope: ingest_hermes.py read-only,
  Embedder interface + DeterministicEmbedder + lazy SentenceTransformerEmbedder,
  get_embedder factory, VectorStore embedder param, cortex_rules.json backend
  key, run_cortex.sh --from-hermes, new tests).
- Fired Lekiu: cortex-v02-ingest (background proc_3f223b35fa2c, 900s).

## Hard constraints for Lekiu
- READ-ONLY on ~/.hermes/state.db (never write/mutate). Test proves no write.
- No forced heavy deps (sentence-transformers lazy + opt-in only).
- Keep 54 existing tests green.

## Run result (cortex-v02-ingest) — SUCCESS (with Jebat fix)
- Lekiu timed out at 900s (SIGTERM, exit 143) but had written all files:
  core/ingest_hermes.py, tests/test_embedder.py, tests/test_ingest_hermes.py,
  modified rules.py/vector_store.py/run_cortex.sh/cortex_rules.json.
- Jebat validated independently: 97 tests green; --from-hermes E2E OK.
- **Issue #1 found (stale-state leak):** digest showed degenerate edge
  `core/memory_curator.py --writes_to--> writes`. Jebat proved via DB query it
  was 1 stale triple from v0.1 sample run (meta.ts matched sample timestamps,
  source=None), NOT a code defect. Current extract_relations code correct (0
  degenerate on fresh export). Root cause = no fresh-state reset; curator
  appends to existing .db so old junk survives.
- **Jebat fix:** run_cortex.sh now `rm -f data/vector_store.db
  data/graph_store.db` before curator (gitignored, regenerated, reversible).
  Re-run --from-hermes: 0 degenerate edges, 3256 clean edges, state.db UNCHANGED.
- Added regression test TestNoDegenerateEdges (cue word never becomes object).
- Suite now 98 green. lekiu_validate.sh: PASSED.

## Delivered (v0.2)
- core/ingest_hermes.py — read-only ~/.hermes/state.db -> JSONL (skips tool/
  empty rows, session/since_ts filters, proves no mutation)
- core/rules.py — Embedder interface + DeterministicEmbedder + lazy
  SentenceTransformerEmbedder + get_embedder() factory (default deterministic)
- core/vector_store.py — accepts embedder= param (no hardcoded embed)
- config/cortex_rules.json — embedding.backend key (default "deterministic")
- run_cortex.sh — --from-hermes mode (read-only) + stale-state reset
- tests/test_embedder.py, tests/test_ingest_hermes.py, +regression test

## Caveats
- Graphify not installed (fallback only).
- Python 3.9.6.
- sentence-transformers is opt-in (lazy import, not installed -> deterministic).
- Retrieval quality limited by deterministic embed (by design); better semantic
  search needs opt-in sentence-transformers backend.
- --from-hermes touches ~/.hermes/state.db but is READ-ONLY (proven unchanged).

## Done
- v0.2 complete + verified + fixed. Ready to commit.
